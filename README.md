# CoolWrist-Main-Repository
Algorithm: An early version of the algorithm. It's since been added to the Firmware under the predict file, as well as modified to reflect the final product.

App: The code for the app. If opened in it's own file and terminal, the app can be run using the AppCode file. 

Firmware: the firmware that is flashed to the device. 